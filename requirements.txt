# LangChain ecosystem (core packages only — no langchain meta-package or community)
langchain-core>=0.2.0
langchain-text-splitters>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0
langchain-litellm>=0.1.0
langgraph>=0.1.0
langsmith>=0.1.59          # observability tracing
litellm>=1.0.0

# Embeddings — fastembed (ONNX, no PyTorch) replaces sentence-transformers
fastembed>=0.3.0

# Storage / DB
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0       # PostgreSQL driver for Supabase
# pgvector support
pgvector>=0.2.0
langchain-postgres>=0.0.12
redis>=5.0                 # optional: Redis semantic cache backend (SEMANTIC_CACHE_BACKEND=redis)

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9      # required for FastAPI Form/File upload endpoints

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Frontend
streamlit>=1.35.0

# Document ingestion
pymupdf>=1.24.4            # PDF parsing
pytesseract>=0.3.10
Pillow>=10.3.0
python-docx>=1.1.0
lxml>=5.0.0

# Web research (Phase 2)
beautifulsoup4>=4.12.0
ddgs>=9.0.0
requests>=2.31.0

# Testing
pytest>=8.0.0
ragas>=0.1.9               # eval/benchmarking — scripts and tests only
